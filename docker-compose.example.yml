version: '3.8'

services:
  gemini-agent:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./:/codebase
      - ${SSH_AUTH_SOCK}:/ssh-agent
    environment:
      - GOOGLE_API_KEY
      - SSH_AUTH_SOCK=/ssh-agent
