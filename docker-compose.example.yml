version: '3.8'

services:
  gemini-agent:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./:/codebase
      - ./config:/config
      - ${SSH_AUTH_SOCK}:/ssh-agent
      # Uncomment the next line to share Gemini CLI OAuth credentials from your host machine
      # - ~/.gemini:/root/.gemini
    environment:
      - GOOGLE_API_KEY
      # - CHAT_HISTORY_FILE=/config/chat_history.json
      # - JULES_TASKS_FILE=/config/tasks.json
      - SSH_AUTH_SOCK=/ssh-agent
      - CHROMA_HOST=chromadb
      - CHROMA_PORT=8000
    depends_on:
      - kotlin-lsp
      - chromadb

  kotlin-lsp:
    build:
      context: .
      dockerfile: Dockerfile.kotlin-lsp
    ports:
      - "5008:5008"
    volumes:
      - ./:/codebase

  chromadb:
    image: chromadb/chroma:latest
    ports:
      - "8000:8000"
    volumes:
      - ./shared_chroma_data:/chroma/chroma
